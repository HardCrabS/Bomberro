using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class BestTimeManager : MonoBehaviour 
{
	// Use this for initialization
	/*void Start () 
    {
        Cloud.OnInitializeComplete += CloudOnceInitializationComplete;
        Cloud.Initialize(false, true);	
	}
	
    public void CloudOnceInitializationComplete()
    {
        Cloud.OnInitializeComplete -= CloudOnceInitializationComplete;
        Debug.LogWarning("Initialization complete");
    }

    public void UpdateLeaderboard(float time)
    {
        Leaderboards.Survivors.SubmitScore((long)time); 
    }*/
}
